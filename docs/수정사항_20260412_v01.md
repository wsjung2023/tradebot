# 자동매매 시스템 수정 계획서 v02
작성일: 2026-04-12  
검증일: 2026-04-12 (소스코드 실측 대조 + 목표 아키텍처 재정의)

---

## 목표 아키텍처 (최종 확정)

```
[30분 cron] — scanJob
  └─ 뒷차기2(seq=30) 조건검색
       ├─ DART 위험공시 필터 (위험 종목 제외)
       ├─ 재무 필터: 3년 매출우상향 + 손익개선 (규칙 기반, GPT 없음)
       ├─ 세력주 필터: 최근 N일 내 거래량 스파이크 존재 여부
       └─ candidate_stocks DB 저장 (이전 결과 초기화 후)

[1분 cron] — tradingJob
  └─ candidate_stocks 로드
       └─ 레인보우 라인 체크 (차트 조회, GPT 없음)
            ├─ 미보유 + 해당 CL선 unitCount > 0 → 신규 매수 (unitCount 유닛)
            ├─ 보유중 + 매수한 CL선보다 1단계 이하로 하락 → 추가매수 (설정 유닛 수)
            └─ 보유중 + 익절% 초과 → 매도

[매일 16:00] — learningJob
  └─ 거래 기록 + 종목 정보 → winrate 분석 → 모델 파라미터 자동 조정
```

**GPT-4 완전 제거** — 30분 스캔, 1분 사이클 모두 GPT 없음. 비용 제로.

---

## 레인보우 라인 번호 체계 (혼동 금지)

```
n=0  MAX    percentage=100  #000000  overbought
n=1  10%    percentage=90   #9966CC  sell
n=2  20%    percentage=80   #FF0000  sell
n=3  30%    percentage=70   #FF8C00  sell
n=4  40%    percentage=60   #FFD700  primary-buy
n=5  CL     percentage=50   #00FF00  primary-buy  ← 초록 (Center Line)
n=6  60%    percentage=40   #0000FF  primary-buy  ← 파랑 (blue)
n=7  70%    percentage=30   #000080  scale-in     ← 남색 (navy)
n=8  80%    percentage=20   #9966CC  scale-in     ← 보라 (purple)
n=9  90%    percentage=10   #000000  deep-value   ← 최하위
n=10 MIN    percentage=0    #000000  deep-value
```

`currentLine` = `Math.round(currentPercent / 10) * 10` (10단위 반올림)

매수 대상 라인 (초록부터 아래):
| 라인명 | currentLine 값 |
|--------|---------------|
| 초록 (CL) | 50 |
| 파랑 | 40 |
| 남색 | 30 |
| 보라 | 20 |
| 최하위 | 10 |

---

## 현재 구현 상태 (실측)

### 구현 완료 ✅
- 레인보우 차트 알고리즘 (`rainbow-chart.ts`) — 변경 없음
- DART 위험공시 필터 (`trade-executor.service.ts`)
- 익절/손절/동적청산 (`checkPositionsForExits`)
- 네이버 뉴스 API 감성점수 (`news.service.ts`)
- 야간 학습 서비스 (`learning.service.ts`) — 거래기록 분석 + 가중치 자동조정
- 주문 동기화 (`order-sync.service.ts`)

### 미구현 / 잘못 구현 ❌
| 항목 | 현재 상태 | 목표 |
|------|-----------|------|
| 조건검색 주기 | 1분마다 | 30분마다 |
| GPT-4 호출 | 분당 최대 20회 | 완전 제거 |
| candidate_stocks | 테이블 없음 | DB 테이블 생성 |
| 유닛 기반 매수 | 없음 (금액 비율만) | CL선별 유닛 수 설정 |
| 추가매수 | 없음 | 매수한 CL선보다 1단계 하락 시 |
| 3년 재무 추세 | PER/PBR 스냅샷 + GPT | 규칙 기반 (GPT 없음) |
| 세력주 거래량 | 당일 10만 이상만 | 최근 N일 스파이크 |
| 캔들 하락멈춤 패턴 | 없음 | (Phase 2 이후) |
| accountId 미설정 오발주 | 랜덤 계좌 선택 | 즉시 중단 |

---

## Phase 1 — 즉시 수행 (이번 수정 범위)

> GPT-4 제거 + 30분 스캔 분리 + 유닛 매수 기초 + 오발주 방지

### 1-A. accountId 미설정 오발주 방지 버그 수정

**파일 1:** `server/auto-trading-worker.ts` lines 230~233
```typescript
// ❌ 현재
const targetAccount = targetAccountId
  ? accounts.find((a: any) => a.id === targetAccountId)
  : accounts.find((a: any) => a.isActive);  // 위험: 랜덤 계좌 선택

// ✅ 수정
if (!targetAccountId) {
  await this.setRunState(model.userId, 'paused', model.id, 'no_account_configured', undefined, { cycleId, durationMs: Date.now() - startedAt });
  console.log(`⛔ 모델 ${model.id} 계좌 미설정 — 중단`);
  return;
}
const targetAccount = accounts.find((a: any) => a.id === targetAccountId);
```

**파일 2:** `server/services/trade-executor.service.ts` — 2곳 동일 패턴
```typescript
// executeBuy() line 474~477, checkPositionsForExits() line 130~133
// ❌ 현재: 둘 다 폴백으로 랜덤 계좌 선택
// ✅ 수정: targetAccountId 없으면 return
if (!targetAccountId) { console.log(`⛔ accountId 미설정`); return; }
const activeAccount = accounts.find((a: any) => a.id === targetAccountId);
```

---

### 1-B. candidate_stocks DB 스키마 추가

**파일:** `shared/schema.ts`
```typescript
export const candidateStocks = pgTable("candidate_stocks", {
  id: serial("id").primaryKey(),
  userId: varchar("user_id").notNull().references(() => users.id, { onDelete: 'cascade' }),
  modelId: integer("model_id").notNull(),
  stockCode: text("stock_code").notNull(),
  stockName: text("stock_name").notNull().default(''),
  source: text("source").notNull().default('뒷차기2'),
  // 스캔 시점에 파악한 레인보우 위치 (30분 스캔에서 저장, 1분 사이클에서 참조)
  scannedLine: integer("scanned_line"),       // 스캔 당시 currentLine (10단위)
  scannedAt: timestamp("scanned_at").notNull().defaultNow(),
});

export const insertCandidateStockSchema = createInsertSchema(candidateStocks).omit({ id: true, scannedAt: true });
export type CandidateStock = typeof candidateStocks.$inferSelect;
export type InsertCandidateStock = z.infer<typeof insertCandidateStockSchema>;
```

이후 `npm run db:push` 실행

---

### 1-C. Storage 인터페이스 & 구현

**파일:** `server/storage/interface.ts`
```typescript
upsertCandidateStock(data: InsertCandidateStock): Promise<CandidateStock>;
getCandidateStocks(userId: string, modelId: number): Promise<CandidateStock[]>;
clearCandidateStocks(userId: string, modelId: number): Promise<void>;
```

**파일:** `server/storage/postgres-core.storage.ts`
- `clearCandidateStocks`: `DELETE WHERE userId AND modelId`
- `upsertCandidateStock`: `INSERT ... ON CONFLICT (userId, modelId, stockCode) DO UPDATE scanned_at, scanned_line`
  - unique constraint: `(user_id, model_id, stock_code)`

---

### 1-D. 워커 구조 재편 (`auto-trading-worker.ts`)

**변경 1: scanJob 필드 추가**
```typescript
private scanJob: cron.ScheduledTask | null = null;
```

**변경 2: start()에 scanJob 추가**
```typescript
async start() {
  this.startScanJob('*/30 * * * *');   // 30분: 뒷차기2 스캔
  this.startTradingJob('* * * * *');   // 1분: 레인보우 체크
  this.startLearningJob('0 16 * * *'); // 16시: 야간 학습
}
```

**변경 3: startScanJob() + runScanCycle() 신규**
```typescript
startScanJob(schedule: string) {
  if (this.scanJob) this.scanJob.stop();
  this.scanJob = cron.schedule(schedule, async () => { await this.runScanCycle(); });
}

private async runScanCycle() {
  if (!isKoreanMarketOpen()) return;
  const models = await storage.getActiveAiModels();
  for (const model of models) {
    const config = (model.config as any) || {};
    if (!config.accountId) continue;
    try {
      const results = await this.userKiwoomService.runCondition(model.userId, '30');
      if (!results?.length) continue;
      await storage.clearCandidateStocks(model.userId, model.id);
      for (const raw of results) {
        const stock = this.userKiwoomService.normalizeConditionResult(raw);
        // DART 위험공시 체크
        const isDangerous = await this.checkDartDanger(model.userId, stock.stockCode);
        if (isDangerous) continue;
        // 재무 필터 (Phase 1: 기본 거래량 체크. Phase 2: 3년 추세)
        await storage.upsertCandidateStock({
          userId: model.userId, modelId: model.id,
          stockCode: stock.stockCode, stockName: stock.stockName,
          source: '뒷차기2',
        });
      }
    } catch (err) { console.error(`[ScanJob] 모델 ${model.id}:`, err); }
  }
}
```

**변경 4: 1분 사이클 — 조건검색 루프 제거, evaluateCandidates로 교체**
```typescript
// ❌ 제거 (lines 297~310)
// const allConditions = ...
// for (const condition of conditions) { await this.processCondition(...) }

// ✅ 대체
const candidates = await storage.getCandidateStocks(model.userId, model.id);
if (!candidates.length) {
  console.log(`  📭 후보 없음 — 30분 스캔 대기 중`);
  return;
}
for (const candidate of candidates) {
  await this.executor.evaluateCandidateStock(model, settings, candidate, kiwoomService);
}
```

**변경 5: processCondition() 메서드 삭제** (lines 368~411)

---

### 1-E. trade-executor.service.ts 수정

**수정 1: executeBuy() 중복 매수 방지 추가**
```typescript
// holdings 조회 직후 (line ~483 이후)
const holdings = await storage.getHoldings(activeAccount.id);
const alreadyHolding = holdings.find(h => h.stockCode === stock.code);
if (alreadyHolding) {
  console.log(`    ⏭️  ${stock.code} 이미 보유 중 — 신규 매수 건너뜀`);
  return;
}
if (maxPositions && maxPositions > 0 && holdings.length >= maxPositions) { ... return; }
```

**수정 2: evaluateCandidateStock() 신규 추가 (GPT 없음)**
```typescript
async evaluateCandidateStock(
  model: AiModel, settings: AutoTradingSettings,
  candidate: CandidateStock, kiwoomService: KiwoomService
): Promise<void> {
  const priceData = await kiwoomService.getStockPrice(candidate.stockCode);
  const price = parseFloat(priceData.output?.stck_prpr ?? '0');
  if (!price) return;
  const stock = { code: candidate.stockCode, name: candidate.stockName, price };

  const config = (model.config as any) || {};
  if (!config.accountId) return;
  const accounts = await storage.getKiwoomAccounts(model.userId);
  const activeAccount = accounts.find((a: any) => a.id === config.accountId);
  if (!activeAccount) return;

  const holdings = await storage.getHoldings(activeAccount.id);
  const existingHolding = holdings.find(h => h.stockCode === stock.code);

  const rainbowEval = await this.evaluate10LineRainbow(stock, settings, kiwoomService);
  const { currentLine } = rainbowEval;

  if (!existingHolding) {
    // 미보유: 해당 CL선에 unitCount > 0이면 신규 매수
    const unitCount = this.getUnitCountForLine(currentLine, config);
    if (unitCount > 0 && currentLine <= 50) {  // 초록(50) 이하
      await this.executeBuy(model, settings, stock, rainbowEval, null as any, kiwoomService);
    }
  } else {
    // 보유중: 매수한 CL선보다 한 단계 이하로 내려왔으면 추가매수
    const entryLine = existingHolding.entryRainbowLine ?? candidate.scannedLine ?? 50;
    const nextLowerLine = entryLine - 10;
    if (currentLine <= nextLowerLine) {
      await this.executeAdditionalBuy(model, settings, stock, existingHolding, rainbowEval, kiwoomService);
    }
    // 익절: checkPositionsForExits()에서 처리 (기존 유지)
  }
}

// 설정에서 해당 CL선의 유닛 수 조회
private getUnitCountForLine(currentLine: number, config: any): number {
  const lineUnits: Record<number, number> = config.lineUnits || {};
  // 기본값: 초록(50)=1, 파랑(40)=1, 남색(30)=1, 보라(20)=1, 최하위(10)=1
  return lineUnits[currentLine] ?? 1;
}
```

**수정 3: executeAdditionalBuy() 신규 추가**
```typescript
async executeAdditionalBuy(
  model: AiModel, settings: AutoTradingSettings,
  stock: { code: string; name: string; price: number },
  holding: any, rainbow: RainbowEval, kiwoomService: KiwoomService
): Promise<void> {
  const config = (model.config as any) || {};
  if (!config.accountId) return;
  const accounts = await storage.getKiwoomAccounts(model.userId);
  const activeAccount = accounts.find((a: any) => a.id === config.accountId);
  if (!activeAccount) return;

  // 이미 이 CL선에서 추가매수 했는지 체크 (오늘 주문 기록 확인)
  // 일일 최대 거래 횟수 체크
  const maxDailyTrades = parseInt(settings.maxDailyTrades?.toString() || '0');
  if (maxDailyTrades > 0) {
    const todayCount = await this.countTodayAutoTrades(activeAccount.id);
    if (todayCount >= maxDailyTrades) return;
  }

  const unitSize = config.unitSize
    ? parseFloat(String(config.unitSize))
    : parseFloat(settings.defaultPositionSize.toString());
  const unitCount = this.getUnitCountForLine(rainbow.currentLine, config);
  const quantity = Math.floor((unitSize * unitCount) / stock.price);
  if (quantity === 0) return;

  await storage.createOrder({ accountId: activeAccount.id, stockCode: stock.code, stockName: stock.name,
    orderType: 'buy', orderMethod: 'market', orderPrice: stock.price.toFixed(2),
    orderQuantity: quantity, isAutoTrading: true, aiModelId: model.id });
  await kiwoomService.placeOrder({ accountNumber: activeAccount.accountNumber,
    stockCode: stock.code, orderType: 'buy', orderQuantity: quantity,
    orderPrice: stock.price, orderMethod: 'market' });
  console.log(`    ✅ 추가매수: ${stock.name} ${quantity}주 @ ${stock.price}원 (${rainbow.currentLine}% 라인, ${unitCount}유닛)`);
}
```

---

### 1-F. job-manager.ts scanJob 연동

**파일:** `server/job-manager.ts`
```typescript
// startJob('auto-trading') 및 setInterval() 재시작 시 scanJob도 함께
autoTradingWorker.startScanJob('*/30 * * * *');
autoTradingWorker.startTradingJob(schedule);
```

---

## Phase 2 — 이후 작업 (별도 진행)

> Phase 1 완료 후 안정화 확인 뒤 진행

### 2-A. 3년 재무 추세 필터 (규칙 기반, GPT 없음)
- **대상 파일:** `server/services/trade-executor.service.ts` `runScanCycle()`
- **방법:** 키움 `getFinancialStatement()` API로 연간 매출/영업이익 3년치 조회
  - 매출 3년 우상향 (`revenue[2] > revenue[1] > revenue[0]`) → pass
  - 영업이익 2년 연속 개선 → pass
  - 둘 다 pass 아니면 candidate에서 제외

### 2-B. 세력주 거래량 스파이크 필터
- **방법:** 일봉 차트 60일 조회 → 최근 20일 내 거래량이 60일 평균의 N배 초과 일수 체크
  - 설정: `volumeSpikeMultiplier` (기본 3배), `volumeSpikeDays` (기본 최소 1일)

### 2-C. 캔들 하락멈춤 패턴 (확률 기반)
- **방법:** 일봉 최근 5~10개 캔들 분석
  - 하락 후 도지/망치 캔들 감지
  - 분봉(5분봉) 추세 반전 확인
- **우선순위 낮음** — Phase 1, 2-A, 2-B 이후

### 2-D. 유닛 수 UI 설정
- AI 모델 설정 화면에 `lineUnits` 설정 추가 (초/파/남/보/최하위 각각 몇 유닛)
- `unitSize` (1유닛 = 얼마) 설정 추가

---

## Phase 1 작업 순서

```
Step 1  shared/schema.ts — candidateStocks 테이블 추가 (scannedLine 포함)
Step 2  npm run db:push
Step 3  server/storage/interface.ts — 3개 메서드 추가
Step 4  server/storage/postgres-core.storage.ts — 3개 메서드 구현
Step 5  server/auto-trading-worker.ts
          a. accountId 미설정 중단 (1-A)
          b. scanJob 필드 선언
          c. start()에 startScanJob 추가
          d. startScanJob() + runScanCycle() 구현
          e. 1분 사이클: 조건검색 루프 → evaluateCandidates로 교체
          f. processCondition() 삭제
Step 6  server/services/trade-executor.service.ts
          a. accountId 폴백 제거 2곳 (1-A)
          b. executeBuy() 중복매수 방지
          c. evaluateCandidateStock() 신규
          d. executeAdditionalBuy() 신규
          e. getUnitCountForLine() 헬퍼 추가
Step 7  server/job-manager.ts — scanJob 연동
Step 8  npm run build + 재시작 + 로그 확인
```

---

## 수정하지 않는 것

| 파일 | 이유 |
|------|------|
| `server/formula/rainbow-chart.ts` | 알고리즘 정확, 변경 없음 |
| `server/services/ai.service.ts` | 수동 분석 기능 유지 (대시보드용) |
| `server/routes/autotrading.routes.ts` `backattack-scan` | 수동 테스트용 엔드포인트 유지 |
| `server/services/learning.service.ts` | Phase 1 변경 없음 |
| `server/services/order-sync.service.ts` | 변경 없음 |

---

## Phase 1 완료 후 예상 효과

| 항목 | 현재 | Phase 1 완료 후 |
|------|------|----------------|
| 조건검색 주기 | 매 1분 | 30분 1회 |
| GPT-4 호출 | 분당 최대 20회 | 0회 |
| 오발주 위험 | accountId 없으면 실계좌 랜덤 | 즉시 중단 |
| 추가매수 | 없음 | 다음 CL선 하락 시 |
| 중복 신규 매수 | 보유 중에도 재매수 가능 | 차단 |
| compute 비용 | $49+ / 기간 | 대폭 감소 예상 |
