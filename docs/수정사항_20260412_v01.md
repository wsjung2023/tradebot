# 자동매매 시스템 수정 계획서 v01 (정밀 검증 완료)
작성일: 2026-04-12  
검증일: 2026-04-12 (소스코드 실측 대조)
상태: 계획 단계 (미착수)

---

## ⚠️ 초안 대비 수정된 사항 (정밀 점검 결과)

| 항목 | 초안 내용 | 실측 결과 |
|------|-----------|-----------|
| 1-A KIWOOM_APP_KEY_MOCK | "현재 버그" | **이미 수정 완료** — 건드릴 것 없음 |
| 파/남/보 threshold | `currentLine <= 20` (파랑) | **잘못됨** → 파랑=40, 파/남/보 이하=`<= 40` |
| scanJob 필드 | 언급 없음 | **누락** — `private scanJob` 필드 선언 필요 |
| job-manager.ts | 언급 없음 | **누락** — job-manager도 수정 대상 |
| 30분 스캔 시 AI/DART | 불명확 | **명시 필요** — 30분에 DART 체크, 1분에는 GPT 없음 |
| backattack-scan 라우트 활용 | 언급 없음 | **활용 가능** — 이미 seq=30 로직 구현되어 있음 |

---

## 레인보우 라인 번호 체계 (필수 이해 — 혼동 금지)

```
rainbow-chart.ts의 lines[] 배열 (percentage = 바닥 기준 0~100%)

n=0  MAX    percentage=100  #000000  overbought   ← 현재가 여기 = currentLine=100
n=1  10%    percentage=90   #9966CC  sell
n=2  20%    percentage=80   #FF0000  sell
n=3  30%    percentage=70   #FF8C00  sell
n=4  40%    percentage=60   #FFD700  primary-buy
n=5  CL     percentage=50   #00FF00  primary-buy  ← Center Line (초록, 중간)
n=6  60%    percentage=40   #0000FF  primary-buy  ← 파랑(blue)
n=7  70%    percentage=30   #000080  scale-in     ← 남색(navy)
n=8  80%    percentage=20   #9966CC  scale-in     ← 보라(purple)
n=9  90%    percentage=10   #000000  deep-value
n=10 MIN    percentage=0    #000000  deep-value   ← 현재가 여기 = currentLine=0
```

`trade-executor.ts`에서 `currentLine` = `Math.round(currentPercent / 10) * 10` (10단위 반올림)

- **CL 위** = `currentLine >= 60` (percentage 60 이상)
- **CL 아래** = `currentLine <= 40` (percentage 40 이하)
- **파랑(60% 라인) 이하** = `currentLine <= 40`
- **남색(70% 라인) 이하** = `currentLine <= 30`
- **보라(80% 라인) 이하** = `currentLine <= 20`
- **파/남/보 이하 (추가매수 조건)** = `currentLine <= 40`

> ❗ 초안의 `currentLine <= 20`은 "파랑"이 아니라 "보라" 이하임. 신규 매수와 추가 매수 조건 모두 **`<= 40`** 이 올바름

---

## 우선순위 1 — 즉시 위험 제거 (실계좌 오발주 방지)

### 1-A. ~~KIWOOM_APP_KEY_MOCK 참조 제거~~ → **이미 완료됨**
**파일:** `server/auto-trading-worker.ts` lines 249~252  
현재 코드 확인:
```typescript
// ✅ 이미 올바르게 수정된 상태 — 추가 작업 불필요
if (targetAccount.accountType !== 'real') {
  appKey = process.env.KIWOOM_APP_KEY;
  appSecret = process.env.KIWOOM_APP_SECRET;
}
```
**서버 코드 전체에서 `KIWOOM_APP_KEY_MOCK` / `KIWOOM_APP_SECRET_MOCK` 참조 없음 확인됨.**  
에이전트(`agent/kiwoom-agent.py`)의 MOCK 변수는 에이전트 내부 분기용으로 건드리지 않음.

---

### 1-B. AI 모델 `accountId` 미설정 시 실계좌 랜덤 선택 버그 수정

**파일:** `server/auto-trading-worker.ts` lines 230~233  

```typescript
// ❌ 현재 — accountId 없으면 isActive인 첫 번째 계좌 자동 선택 → 실계좌 선택 위험
const targetAccountId = (model.config as any)?.accountId;
const targetAccount = targetAccountId
  ? accounts.find((a: any) => a.id === targetAccountId)
  : accounts.find((a: any) => a.isActive);  // ← 위험: 어떤 계좌든 랜덤 선택

// ✅ 수정 후 — accountId 없으면 즉시 중단
const targetAccountId = (model.config as any)?.accountId;
if (!targetAccountId) {
  await this.setRunState(model.userId, 'paused', model.id, 'no_account_configured', undefined, {
    cycleId, durationMs: Date.now() - startedAt,
  });
  console.log(`⛔ AI 모델 ${model.id}(${model.modelName})에 계좌 미설정 — 중단. 관리자 > AI모델에서 계좌 선택 필요`);
  return;
}
const targetAccount = accounts.find((a: any) => a.id === targetAccountId);
```

**파일:** `server/services/trade-executor.service.ts` — 동일 패턴 2곳  

```typescript
// 수정 위치 1: executeBuy() lines 474~477
// 수정 위치 2: checkPositionsForExits() lines 130~133

// ❌ 현재 두 곳 모두
const activeAccount = targetAccountId
  ? accounts.find((a: any) => a.id === targetAccountId)
  : accounts.find((a: any) => a.isActive);  // ← 위험

// ✅ 수정 후 (두 곳 동일)
if (!targetAccountId) {
  console.log(`⛔ accountId 미설정 — 실행 중단`);
  return;
}
const activeAccount = accounts.find((a: any) => a.id === targetAccountId);
```

---

## 우선순위 2 — 자동매매 아키텍처 재구성

### 목표 구조
```
[30분 cron: */30 * * * *] — scanJob (NEW)
  └─ AI 모델별 뒷차기2(seq=30) 조건검색 (에이전트)
       └─ DART 위험 공시 필터 (위험 종목 제외)
            └─ candidate_stocks DB에 저장 (이전 결과 초기화 후)

[1분 cron: * * * * *] — tradingJob (기존, 내용 교체)
  └─ candidate_stocks에서 후보 종목 로드
       └─ 레인보우 라인 체크 (차트 조회 — 에이전트)
            ├─ 미보유 + currentLine <= 40 (파랑 이하) → 신규 매수
            ├─ 보유중 + currentLine <= 40 (파/남/보 이하) → 추가 매수
            └─ 보유중 + currentLine >= 60 (CL 위) → 매도 검토
```

> **GPT-4 호출**: 30분 스캔에서 제거 (DART 체크는 유지), 1분 사이클에서도 제거 → 비용 절감

---

### 2-A. DB 스키마 추가: `candidate_stocks` 테이블
**파일:** `shared/schema.ts`

```typescript
export const candidateStocks = pgTable("candidate_stocks", {
  id: serial("id").primaryKey(),
  userId: varchar("user_id").notNull().references(() => users.id, { onDelete: 'cascade' }),
  modelId: integer("model_id").notNull(),
  stockCode: text("stock_code").notNull(),
  stockName: text("stock_name").notNull().default(''),
  source: text("source").notNull().default('뒷차기2'),
  scannedAt: timestamp("scanned_at").notNull().defaultNow(),
});

export const insertCandidateStockSchema = createInsertSchema(candidateStocks).omit({ id: true, scannedAt: true });
export type CandidateStock = typeof candidateStocks.$inferSelect;
export type InsertCandidateStock = z.infer<typeof insertCandidateStockSchema>;
```

> 이후 `npm run db:push` 실행

---

### 2-B. Storage 인터페이스 & 구현 추가
**파일:** `server/storage/interface.ts`

```typescript
upsertCandidateStock(data: InsertCandidateStock): Promise<CandidateStock>;
getCandidateStocks(userId: string, modelId: number): Promise<CandidateStock[]>;
clearCandidateStocks(userId: string, modelId: number): Promise<void>;
```

**파일:** `server/storage/postgres-core.storage.ts`
- `clearCandidateStocks`: `DELETE FROM candidate_stocks WHERE user_id=? AND model_id=?`
- `upsertCandidateStock`: `INSERT ... ON CONFLICT (user_id, model_id, stock_code) DO UPDATE SET scanned_at=NOW()`
  - unique constraint: `(userId, modelId, stockCode)` 필요

---

### 2-C. 워커 구조 재편
**파일:** `server/auto-trading-worker.ts`

**변경 1: `scanJob` 필드 추가** (class 상단)
```typescript
private cronJob: cron.ScheduledTask | null = null;
private scanJob: cron.ScheduledTask | null = null;   // ← 신규 추가
private learningJob: cron.ScheduledTask | null = null;
```

**변경 2: `start()` 메서드에 스캔 잡 추가**
```typescript
async start() {
  this.startScanJob('*/30 * * * *');      // 30분: 뒷차기2 스캔
  this.startTradingJob('* * * * *');      // 1분: 레인보우 체크
  this.startLearningJob('0 16 * * *');
}
```

**변경 3: `startScanJob()` 신규 메서드 추가**
```typescript
startScanJob(schedule: string) {
  if (this.scanJob) this.scanJob.stop();
  this.scanJob = cron.schedule(schedule, async () => {
    await this.runScanCycle();
  });
  console.log(`✅ Scan Job started (schedule: ${schedule})`);
}

private async runScanCycle() {
  if (!isKoreanMarketOpen()) return;
  const models = await storage.getActiveAiModels();
  for (const model of models) {
    const config = (model.config as any) || {};
    if (!config.accountId) continue;  // accountId 없으면 스킵
    try {
      // 1. 뒷차기2(seq=30) 조건검색 실행 (에이전트)
      const results = await this.userKiwoomService.runCondition(model.userId, '30');
      if (!results?.length) continue;

      // 2. 이전 후보 초기화
      await storage.clearCandidateStocks(model.userId, model.id);

      // 3. DART 위험 공시 필터 후 저장
      for (const raw of results) {
        const stock = this.userKiwoomService.normalizeConditionResult(raw);
        const isDangerous = await this.checkDartDanger(stock.stockCode);
        if (isDangerous) continue;
        await storage.upsertCandidateStock({
          userId: model.userId,
          modelId: model.id,
          stockCode: stock.stockCode,
          stockName: stock.stockName,
          source: '뒷차기2',
        });
      }
      console.log(`[ScanJob] 모델 ${model.id} 후보 저장 완료`);
    } catch (err) {
      console.error(`[ScanJob] 모델 ${model.id} 스캔 오류:`, err);
    }
  }
}
```

**변경 4: 1분 사이클에서 조건검색 로직 교체**
```typescript
// ❌ 제거: lines 297~310 전체
// const allConditions = await storage.getConditionFormulas(model.userId);
// ... for (const condition of conditions) { ... }

// ✅ 대체
const candidates = await storage.getCandidateStocks(model.userId, model.id);
if (!candidates.length) {
  console.log(`  📭 후보 종목 없음 — 30분 스캔 대기 중`);
  return;
}
for (const candidate of candidates) {
  await this.executor.evaluateCandidateStock(model, settings, candidate, kiwoomService);
}
```

**변경 5: `processCondition()` 메서드 삭제** (lines 379~423)

---

### 2-D. `job-manager.ts` 스캔 잡 추가
**파일:** `server/job-manager.ts`

현재 `auto-trading` 잡 시작/재시작 처리 부분에 scanJob 연동 추가:
```typescript
// 기존 (line 116, 159 근처)
autoTradingWorker.startTradingJob(schedule);

// 수정 후: scanJob도 함께 시작
autoTradingWorker.startScanJob('*/30 * * * *');
autoTradingWorker.startTradingJob(schedule);
```

---

### 2-E. `trade-executor.service.ts` 신규 메서드 추가

**`evaluateCandidateStock()` 신규 추가** (GPT-4 없음, 레인보우 + 보유 체크만)

```typescript
async evaluateCandidateStock(
  model: AiModel,
  settings: AutoTradingSettings,
  candidate: CandidateStock,
  kiwoomService: KiwoomService
): Promise<void> {
  try {
    // 1. 현재가 조회
    const priceData = await kiwoomService.getStockPrice(candidate.stockCode);
    const price = parseFloat(priceData.output?.stck_prpr ?? '0');
    if (!price) return;
    const stock = { code: candidate.stockCode, name: candidate.stockName, price };

    // 2. 계좌 & 보유 여부 확인
    const config = (model.config as any) || {};
    if (!config.accountId) return;
    const accounts = await storage.getKiwoomAccounts(model.userId);
    const activeAccount = accounts.find((a: any) => a.id === config.accountId);
    if (!activeAccount) return;
    const holdings = await storage.getHoldings(activeAccount.id);
    const existingHolding = holdings.find(h => h.stockCode === stock.code);

    // 3. 레인보우 라인 계산
    const rainbowEval = await this.evaluate10LineRainbow(stock, settings, kiwoomService);
    const { currentLine, action } = rainbowEval;

    if (!existingHolding) {
      // ── 미보유: 파/남/보 이하(CL 아래) → 신규 매수
      if (action === 'buy' && currentLine <= 40) {
        await this.executeBuy(model, settings, stock, rainbowEval, null as any, kiwoomService);
      }
    } else {
      // ── 보유중: 파/남/보 이하 → 추가 매수
      if (currentLine <= 40) {
        await this.executeAdditionalBuy(model, settings, stock, existingHolding, rainbowEval, kiwoomService);
      }
      // ── 보유중: CL 위(초록↑) → 매도 검토
      else if (action === 'sell' && currentLine >= 60) {
        await this.executeSell(model, settings, stock, rainbowEval, kiwoomService);
      }
    }
  } catch (err) {
    console.error(`[evaluateCandidateStock] ${candidate.stockCode} 오류:`, err);
  }
}
```

**`executeAdditionalBuy()` 신규 추가**

```typescript
async executeAdditionalBuy(
  model: AiModel,
  settings: AutoTradingSettings,
  stock: { code: string; name: string; price: number },
  holding: any,
  rainbow: RainbowEval,
  kiwoomService: KiwoomService
): Promise<void> {
  console.log(`    ➕ 추가매수: ${stock.name}(${stock.code}) @ ${rainbow.currentLine}% 라인`);
  try {
    const config = (model.config as any) || {};
    if (!config.accountId) return;
    const accounts = await storage.getKiwoomAccounts(model.userId);
    const activeAccount = accounts.find((a: any) => a.id === config.accountId);
    if (!activeAccount) return;

    // 일일 최대 거래 횟수 체크
    const maxDailyTrades = parseInt(settings.maxDailyTrades?.toString() || '0');
    if (maxDailyTrades > 0) {
      const todayCount = await this.countTodayAutoTrades(activeAccount.id);
      if (todayCount >= maxDailyTrades) {
        console.log(`    ⚠️  일일 최대 거래 횟수 초과 — 추가매수 건너뜀`);
        return;
      }
    }

    // 추가 매수 수량: 기본 포지션 사이즈의 50%
    const baseSize = parseFloat(settings.defaultPositionSize.toString());
    const quantity = Math.floor((baseSize * 0.5) / stock.price);
    if (quantity === 0) return;

    const order = await storage.createOrder({
      accountId: activeAccount.id,
      stockCode: stock.code,
      stockName: stock.name,
      orderType: 'buy',
      orderMethod: 'market',
      orderPrice: stock.price.toFixed(2),
      orderQuantity: quantity,
      isAutoTrading: true,
      aiModelId: model.id,
    });

    await kiwoomService.placeOrder({
      accountNumber: activeAccount.accountNumber,
      stockCode: stock.code,
      orderType: 'buy',
      orderQuantity: quantity,
      orderPrice: stock.price,
      orderMethod: 'market',
    });

    console.log(`    ✅ 추가매수 완료: ${stock.name} ${quantity}주 @ ${stock.price}원`);
  } catch (err) {
    console.error(`    ❌ 추가매수 실패 ${stock.code}:`, err);
  }
}
```

---

## 우선순위 3 — 중복 매수 방지 (신규 매수에서)

**파일:** `server/services/trade-executor.service.ts`  
**위치:** `executeBuy()` 내부 (line 483 이후, holdings 조회 직후)

```typescript
// ── 최대 보유 종목 체크 ── (기존 유지)
const maxPositions = config.maxPositions != null ? parseInt(String(config.maxPositions)) : null;
if (maxPositions && maxPositions > 0) {
  const holdings = await storage.getHoldings(activeAccount.id);
  if (holdings.length >= maxPositions) { ... return; }

  // ← 이 자리에 추가
  // ── 이미 보유 중인 종목 중복 매수 차단 ──
  const alreadyHolding = holdings.find(h => h.stockCode === stock.code);
  if (alreadyHolding) {
    console.log(`    ⏭️  ${stock.code} 이미 보유 중 (${alreadyHolding.quantity}주) — 신규 매수 건너뜀`);
    return;
  }
}
```

> `maxPositions` 설정이 없는 경우에도 중복 체크가 필요하므로:
```typescript
// maxPositions 설정 없을 때도 중복 체크
const holdings = await storage.getHoldings(activeAccount.id);
const alreadyHolding = holdings.find(h => h.stockCode === stock.code);
if (alreadyHolding) {
  console.log(`    ⏭️  ${stock.code} 이미 보유 중 — 신규 매수 건너뜀`);
  return;
}
if (maxPositions && maxPositions > 0 && holdings.length >= maxPositions) { ... return; }
```

---

## 작업 순서 (의존성 고려)

```
Step 1  shared/schema.ts — candidateStocks 테이블 추가
Step 2  npm run db:push 실행
Step 3  server/storage/interface.ts — 3개 메서드 시그니처 추가
Step 4  server/storage/postgres-core.storage.ts — 3개 메서드 구현
Step 5  server/auto-trading-worker.ts
          5-a. 1-B 수정: accountId 미설정 시 중단 (line 230~233)
          5-b. scanJob 필드 추가
          5-c. start()에 startScanJob() 추가
          5-d. startScanJob() + runScanCycle() 신규 구현
          5-e. 1분 사이클: 조건검색 루프 제거 → evaluateCandidates() 로 교체
          5-f. processCondition() 메서드 삭제
Step 6  server/services/trade-executor.service.ts
          6-a. 1-B 수정: executeBuy() + checkPositionsForExits() accountId 폴백 제거
          6-b. 우선순위 3: executeBuy() 중복 매수 방지 추가
          6-c. evaluateCandidateStock() 신규 추가
          6-d. executeAdditionalBuy() 신규 추가
Step 7  server/job-manager.ts — startScanJob() 호출 추가
Step 8  npm run build + 서버 재시작 + 로그 확인
```

---

## 수정하지 않는 것

| 파일 | 이유 |
|------|------|
| `server/routes/autotrading.routes.ts` `backattack-scan` | 수동 테스트 엔드포인트 — 유지 (내부 로직 참고용) |
| `server/formula/rainbow-chart.ts` | 알고리즘 변경 없음 |
| `server/services/ai.service.ts` | analyzeStock / integratedAnalysis 유지 (향후 30분 스캔 필터 용도) |
| `server/services/kiwoom_OLD/kiwoom.order.ts` | `dmst_stex_tp: "KRX"` 하드코딩은 현재 실계좌 운용이 주이므로 보류 |

---

## 예상 효과

| 항목 | 현재 | 수정 후 |
|------|------|---------|
| 조건검색 주기 | 1분 (매 사이클) | 30분 |
| 1분 사이클 부하 | 조건검색 + GPT + 레인보우 | 레인보우만 |
| GPT-4 호출 | 매 1분 × 종목 수 | 없음 (DART만 유지) |
| 오발주 위험 | accountId 없으면 실계좌 랜덤 | accountId 없으면 즉시 중단 |
| 추가 매수 | 미구현 | 파/남/보 기반 구현 |
| 중복 매수 | 보유 중에도 재매수 | 보유 확인 후 차단 |
