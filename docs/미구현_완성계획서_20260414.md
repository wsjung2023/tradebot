# 키움 AI 자동매매 — 미구현 완성 계획서

작성일: 2026-04-14  
작성자: 감사 기반 실측 (소스코드 + DB 직접 확인)

---

## 1. 현재 상태 요약 (2026-04-14 실측)

### DB 핵심 테이블 데이터 현황

| 테이블 | 행 수 | 비고 |
|--------|-------|------|
| `trading_performance` | **0** | 실거래 없음, 학습 불가 상태 |
| `trading_logs` | 6 | 테스트 로그만 존재 |
| `candidate_stocks` | **0** | 스캔잡 미실행 |
| `market_issues` | **0** | UI 없어서 등록 불가 |
| `watchlist_signals` | **0** | 신호 생성 미구현 |
| `financial_snapshots` | **0** | 재무 캐시 없음 |

---

## 2. 구현 완료 현황 ✅

이 항목들은 코드 및 실행 확인 완료.

```
핵심 자동매매 파이프라인
  ├─ scanJob (30분): 뒷차기2 조건검색 → DART 필터 → candidate_stocks 저장 ✅
  ├─ tradingJob (1분): evaluateCandidateStock → GPT분석 → 레인보우 → 매수/추가매수 ✅
  ├─ learningJob (16:00): optimizeModel() → winRate 분석 → minAiConfidence 자동조정 ✅
  ├─ comprehensiveAiAnalysis(): GPT 2개 병렬 + 거래량 점수 → confidence ✅
  └─ checkPositionsForExits(): 익절/손절/급등/장기 청산 ✅

자동매매 설정 (AutoTradingSettings)
  ├─ unitSize / lineUnits[N] (레인보우 라인별 유닛) ✅
  ├─ maxPositions / maxDailyTrades ✅
  ├─ takeProfitPercent / stopLoss / surgeThreshold / stalePeriodDays ✅
  ├─ minAiConfidence 슬라이더 ✅
  ├─ themeWeight / newsWeight / financialsWeight / liquidityWeight / institutionalWeight ✅
  ├─ requireGoodFinancials / requireHighLiquidity 스위치 ✅
  └─ requireMarketIssue 스위치 (코드 구현됨, UI도 있음) ✅

AI 분석 페이지
  ├─ /api/ai/analyze-stock → GPT analyzeStock() ✅
  ├─ /api/ai/analyze-portfolio ✅
  └─ /api/ai/integrated-analysis ✅

계좌 / 잔고
  ├─ 계좌 CRUD (dashboard) ✅
  ├─ 잔고 자동갱신 (5분 BalanceRefreshService, KST 08:30~18:00) ✅
  └─ 보유종목 실시간 표시 ✅

주문 / 거래내역
  ├─ 수동 매수/매도 주문 ✅
  ├─ 주문 상태 동기화 (OrderSyncService) ✅
  └─ 오래된 pending 자동 만료 (매매잡 실행 시) ✅

관심종목
  ├─ 종목 추가/삭제 ✅
  └─ 실시간 시세 (WebSocket 스트림) ✅

조건검색 / 뒷차기 스캔
  ├─ 키움 HTS 조건 목록 조회 ✅
  ├─ 조건 실행 (manual) ✅
  └─ backattack-scan 페이지 (수동 실행 + 레인보우 분석) ✅

설정
  ├─ GPT 모델 선택 ✅
  └─ 트레이딩 모드 (실계좌/모의) ✅
```

---

## 3. 미구현 / 미완성 목록 ❌

### M-1. 포트폴리오 페이지 — 완전 미구현

**현황:**  
`client/src/pages/portfolio.tsx` 전체가 placeholder 1개짜리 카드만 표시됨.

```typescript
// 현재 전체 내용
return (
  <Card>
    <CardContent>
      <p>포트폴리오 화면을 구현 중입니다</p>
    </CardContent>
  </Card>
);
```

**구현해야 할 내용:**
- 전체 계좌 통합 보유종목 현황 (계좌별/종목별 분류)
- 종목별 수익률 차트 (매입가 vs 현재가)
- 섹터/업종별 분산도 파이차트
- 전체 포트폴리오 수익률 요약 (총 투자금, 평가금액, 손익)

**우선순위:** 높음 (메뉴에 있는데 아무것도 없음)

---

### M-2. 자산 스냅샷 저장 & 대시보드 자산 추이 차트 — 미구현

**현황:**  
`GET /api/accounts/:accountId/asset-snapshots` 라우트 코드:

```typescript
// 현재는 스냅샷 저장 기능 미구현 → 빈 배열 반환 (가짜 데이터 표시 금지)
res.json([]);
```

대시보드의 "자산 추이" 라인 차트는 쿼리를 날리지만 항상 빈 배열을 받음 → **차트에 아무것도 안 나옴**.

**구현해야 할 내용:**

```
1. 스냅샷 저장 로직
   - BalanceRefreshService의 잔고 갱신 성공 시점마다 asset_snapshots에 저장
     (accountId, totalAssets, totalProfitLoss, totalProfitRate, snapshotAt)
   - DB에 auto_trading_runs 또는 별도 asset_snapshots 테이블 생성

2. 스냅샷 조회 API
   - GET /api/accounts/:accountId/asset-snapshots
   - 파라미터: days=30 (기간)
   - 반환: [{ date, totalAssets, profitLoss }]

3. 대시보드 차트 연결
   - 이미 프론트엔드 코드는 준비됨 (쿼리 + Recharts LineChart)
   - 백엔드 데이터만 연결하면 즉시 표시됨
```

**우선순위:** 높음

---

### M-3. 시장 이슈 종목 관리 UI — 미구현

**현황:**  
`requireMarketIssue` 필터는 코드에 구현됨. ON으로 설정 시 `market_issues` 테이블에서 오늘 날짜로 등록된 종목만 매수 허용.  
그런데 `market_issues` 테이블에 **행이 0개**. 등록하는 UI가 없기 때문.

```sql
SELECT COUNT(*) FROM market_issues;  -- 결과: 0
```

**구현해야 할 내용:**

```
1. 시장 이슈 등록 UI
   - 자동매매 페이지 또는 별도 탭에 "오늘의 시장 이슈 종목" 관리
   - 종목 검색 → 이슈 카테고리 선택 → 저장

2. 자동 등록 옵션
   - backattack-scan 결과에서 "시장 이슈" 체크박스 선택 시 자동 등록
   - DART 공시 키워드 매칭으로 자동 이슈 생성

3. API
   - POST /api/market-issues (date, stockCode, stockName, issueType, description)
   - DELETE /api/market-issues/:id
   - GET /api/market-issues?date=YYYYMMDD (이미 존재)
```

**우선순위:** 중간 (requireMarketIssue = OFF면 영향 없음)

---

### M-4. 시그널 분석 페이지 — 자동 신호 생성 미구현

**현황:**  
`/watchlist-signals` 페이지:
- GET/DELETE API: 2026-04-14 오늘 추가 완료 ✅
- "신호 생성" 버튼(`POST /api/watchlist-signals/generate-all`): **안내 메시지만 반환**, 실제 신호 생성 없음

```typescript
// 현재 generate-all 구현
res.json({
  message: "시그널은 뒷차기 스캔 탭에서 실행하거나 개별 추가하세요",
  generated: 0,
});
```

**구현해야 할 내용:**

```
generate-all 실제 구현:
1. 사용자 관심종목 전체 조회
2. 각 종목 현재가 조회 (키움 에이전트)
3. 레인보우 라인 계산 → currentLine 추출
4. 간단한 신호 로직:
   - currentLine ≤ 30 → buy
   - currentLine ≥ 70 → sell
   - 그 외 → neutral
5. watchlist_signals 테이블에 upsert
   (watchlistId, currentSignal, signalStrength, lastCalculatedAt)
```

**우선순위:** 중간

---

### M-5. Pending 주문 자동 만료 — 매매잡 의존 문제

**현황:**  
`expireOldPendingOrders()`는 `processModel()` 내부에서만 호출됨.  
매매잡이 꺼져 있으면 오래된 pending이 영원히 대기 상태로 남음.

오늘 DB에서 수동 취소 처리 완료:
```sql
UPDATE orders SET order_status = 'cancelled'
WHERE order_status = 'pending' AND created_at < NOW() - INTERVAL '1 day';
-- 결과: 7행 취소
```

**구현해야 할 내용:**

```
서버 시작 시 자동 실행 또는 별도 타이머:
server/index.ts 또는 routes/index.ts에:

// 서버 시작 후 1회 실행 + 이후 1시간 주기
setTimeout(async () => {
  const accounts = await storage.getAllAccounts();
  for (const acc of accounts) {
    await orderSyncService.expireOldPendingOrders(acc.id);
  }
}, 5000);
setInterval(같은 로직, 3600000);  // 1시간마다
```

**우선순위:** 높음 (서비스 재시작 시마다 수동 처리 필요)

---

### M-6. 거래 성과 추적 자동 저장 — ✅ 정상 구현 확인

**현황 (2026-04-14 코드 직접 확인):**  
`executeBuy()` 내 `storage.createTradingPerformance()` 호출 확인됨.  
매수 주문 체결 직후 `entryPrice / entryRainbowLine / aiConfidence / 5개 점수` 저장 완료.  
행 수가 0인 이유: **실제 매수 거래가 아직 한 번도 발생하지 않았기 때문** (키움 에이전트 연결 + 스캔잡 실행 필요).

→ **별도 구현 작업 불필요.** 키움 에이전트 연결 후 정상 작동 예상.

---

### M-7. GPT 프롬프트 사용자 편집 기능 (Phase 2-A)

**현황:** 설계만 완료 (기존 `수정사항_20260412_v01.md` Phase 2-A).  
`ai.service.ts`의 프롬프트는 코드에 하드코딩됨.

**구현 내용:**
```
1. DB: ai_models 또는 ai_model_specs에 analyzeStockPrompt, integratedAnalysisPrompt 컬럼 추가
2. ai.service.ts: 모델별 promptTemplate를 DB에서 읽어 사용
3. UI: 자동매매 설정 > AI 모델 탭에 "프롬프트 편집" 섹션 추가
```

**우선순위:** 낮음

---

### M-8. 3년 재무 추세 필터 (Phase 2-B)

**현황:** 설계만 완료 (기존 `수정사항_20260412_v01.md` Phase 2-B).  
`financial_snapshots` 테이블 존재, 행 수 0.

**구현 내용:**
```
1. 스캔잡 실행 시 각 후보 종목의 재무제표 3년치 조회 + financial_snapshots 저장
2. 필터 조건: 매출 3년 우상향 + 영업이익 2년 개선 시 candidate 통과
3. AutoTradingSettings에 requireStrongFinancialTrend 토글 추가
```

**우선순위:** 낮음

---

### M-9. 거래량 스파이크 필터 (Phase 2-C)

**현황:** 설계만 완료 (기존 `수정사항_20260412_v01.md` Phase 2-C).

**구현 내용:**
```
1. 스캔잡 후보 종목에서 일봉 60일 조회
2. 최근 20일 내 거래량이 60일 평균 대비 N배 이상인 날이 있으면 통과
3. AutoTradingSettings에 volumeSpikeMultiplier 슬라이더 추가
```

**우선순위:** 낮음

---

## 4. 구현 우선순위 및 일정

### 🔴 즉시 해야 할 것 (서비스 안정성)

| # | 항목 | 예상 작업량 |
|---|------|-------------|
| 1 | **M-5**: 서버 시작 시 pending 주문 자동 만료 타이머 추가 | 소 (1시간) |
| ~~2~~ | ~~M-6: trading_performance 저장~~ | ~~불필요 — 코드 정상 확인됨~~ |

### 🟡 단기 개선 (사용성)

| # | 항목 | 예상 작업량 |
|---|------|-------------|
| 3 | **M-2**: 자산 스냅샷 저장 + 대시보드 차트 연결 | 중 (4~6시간) |
| 4 | **M-1**: 포트폴리오 페이지 구현 | 중 (6~8시간) |
| 5 | **M-3**: 시장 이슈 종목 관리 UI | 소 (2~3시간) |

### 🟢 이후 개선

| # | 항목 | 예상 작업량 |
|---|------|-------------|
| 6 | **M-4**: 시그널 자동 생성 (generate-all) | 중 (4시간) |
| 7 | **M-7**: GPT 프롬프트 사용자 편집 | 대 (8시간) |
| 8 | **M-8**: 3년 재무 추세 필터 | 대 (8시간) |
| 9 | **M-9**: 거래량 스파이크 필터 | 중 (4시간) |

---

## 5. 오늘(2026-04-14) 수정 완료 사항

| 항목 | 내용 |
|------|------|
| pending 주문 정리 | 오래된 pending 주문 7건 → DB에서 직접 cancelled 처리 |
| 시그널 분석 API 수정 | `/api/watchlist-signals` GET/DELETE/generate-all 라우트 추가 (프론트가 요청하던 경로로 연결) |
| 가이드/튜토리얼 오류 수정 | analyzeStock 입력 파라미터 오류, requireMarketIssue 누락, maxPositions 위치 오류, 학습 사이클 추측 기재 등 6건 수정 |

---

## 6. 건드리지 않는 것 (이미 정상)

| 파일/기능 | 이유 |
|-----------|------|
| `server/formula/rainbow-chart.ts` | 알고리즘 정확, 변경 없음 |
| `server/services/ai.service.ts` | GPT 호출 정상 |
| `server/services/trade-executor.service.ts` | evaluateCandidateStock 구현 정상 |
| `server/auto-trading-worker.ts` | 잡 스케줄러 정상 |
| `server/services/learning.service.ts` | optimizeModel() 정상 (데이터 쌓여야 실효) |
| `server/services/order-sync.service.ts` | 동기화 로직 정상 |
| `shared/schema.ts` | DB 스키마 완성 |
