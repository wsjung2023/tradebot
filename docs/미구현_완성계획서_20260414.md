# 키움 AI 자동매매 — 미구현 완성 계획서

작성일: 2026-04-14  
최종갱신: 2026-05-09 (코드 실측 기반 전면 업데이트 — Replit→로컬 전환 반영)  
작성자: 감사 기반 실측 (소스코드 + DB 직접 확인)

---

## 0. 환경 변경 사항

> **2026-05-09**: Replit 클라우드 → **로컬 Windows PC** 마이그레이션 완료
> - 서버: `localhost:5000` (Node.js + Vite dev)
> - 에이전트: 같은 PC에서 `python kiwoom-agent.py` (localhost 폴링)
> - DB: 로컬 PostgreSQL (또는 Neon 유지 가능)
> - 외부 접근: Cloudflare Tunnel → 커스텀 도메인
> - 자동 실행: Windows Startup + schtasks (Server / Agent / Caddy)

---

## 1. 현재 상태 요약 (2026-05-09 실측)

### DB 핵심 테이블 데이터 현황 (최초 작성 대비)

| 테이블 | 04-14 실측 | 05-09 상태 | 비고 |
|--------|-----------|-----------|------|
| `trading_performance` | **0** | 실거래 데이터 축적 중 | 학습 가능 |
| `trading_logs` | 6 | 활성 축적 중 | 실거래 로그 |
| `candidate_stocks` | **0** | 스캔잡 정상 가동 중 | evaluationResult/skipReason 포함 |
| `market_issues` | **0** | API 완전 구현 | CRUD 4개 엔드포인트 |
| `watchlist_signals` | **0** | generate-all 완전 구현 | 레인보우 분석 + upsert |
| `financial_snapshots` | **0** | sync-financials API 완전 구현 | 키움 재무데이터 연동 |
| `asset_snapshots` | (없었음) | 스키마 + 저장 + 조회 완성 | BalanceRefreshService 자동 저장 |
| `engine_notifications` | 0 | BUY/SELL/SKIP/ADDITIONAL_BUY 이벤트 저장 | 모니터링 피드 연동 |

---

## 2. 구현 완료 현황 ✅

이 항목들은 코드 및 실행 확인 완료.

```
핵심 자동매매 파이프라인
  ├─ scanJob (30분): 뒷차기2 조건검색 → DART 필터 → candidate_stocks 저장 ✅
  ├─ tradingJob (1분): evaluateCandidateStock → GPT분석 → 레인보우 → 매수/추가매수 ✅
  ├─ learningJob (16:00): optimizeModel() → winRate 분석 → minAiConfidence 자동조정 ✅
  ├─ comprehensiveAiAnalysis(): GPT 2개 병렬 + 거래량 점수 → confidence ✅
  └─ checkPositionsForExits(): 익절/손절/급등/장기 청산 ✅

자동매매 설정 (AutoTradingSettings)
  ├─ unitSize / lineUnits[N] (레인보우 라인별 유닛) ✅
  ├─ maxPositions / maxDailyTrades ✅
  ├─ takeProfitPercent / stopLoss / surgeThreshold / stalePeriodDays ✅
  ├─ minAiConfidence 슬라이더 ✅
  ├─ themeWeight / newsWeight / financialsWeight / liquidityWeight / institutionalWeight ✅
  ├─ requireGoodFinancials / requireHighLiquidity 스위치 ✅
  └─ requireMarketIssue 스위치 (코드 구현됨, UI도 있음) ✅

AI 분석 페이지
  ├─ /api/ai/analyze-stock → GPT analyzeStock() ✅
  ├─ /api/ai/analyze-portfolio ✅
  └─ /api/ai/integrated-analysis ✅

계좌 / 잔고
  ├─ 계좌 CRUD (dashboard) ✅
  ├─ 잔고 자동갱신 (5분 BalanceRefreshService, KST 08:30~18:00) ✅
  └─ 보유종목 실시간 표시 ✅

주문 / 거래내역
  ├─ 수동 매수/매도 주문 ✅
  ├─ 주문 상태 동기화 (OrderSyncService) ✅
  └─ 오래된 pending 자동 만료 (매매잡 실행 시) ✅

관심종목
  ├─ 종목 추가/삭제 ✅
  └─ 실시간 시세 (WebSocket 스트림) ✅

조건검색 / 뒷차기 스캔
  ├─ 키움 HTS 조건 목록 조회 ✅
  ├─ 조건 실행 (manual) ✅
  └─ backattack-scan 페이지 (수동 실행 + 레인보우 분석) ✅

설정
  ├─ GPT 모델 선택 ✅
  └─ 트레이딩 모드 (실계좌/모의) ✅
```

---

## 3. 미구현 항목 — 04-14 → 05-09 구현 현황 업데이트

### M-1. 포트폴리오 페이지 ✅ 구현 완료 (2026-05 완료)

**04-14 현황:** placeholder 1개짜리 카드만 표시  
**05-09 현황:** `portfolio.tsx` 245줄, 완전한 페이지

**구현된 내용:**
- 전체 계좌 통합 보유종목 현황 — `GET /api/portfolio/holdings` (60초 자동갱신)
- 요약 카드 4개: 총 투자금액, 평가금액, 총 손익(수익률), 보유 종목 수
- 계좌 필터: [전체] [계좌1] [계좌2] ...
- 보유종목 테이블: 종목명/코드, 계좌, 수량, 매입가, 현재가, 손익, 수익률
- 백엔드: `storage.getAllHoldingsForUser()` (account.routes.ts:398-406)

**아직 미구현:**
- ~~종목별 수익률 차트 (매입가 vs 현재가)~~ — 별도 결정 필요
- ~~섹터/업종별 분산도 파이차트~~ — 별도 결정 필요
- 보유종목별 레인보우 현재 라인 표시 — 부가 기능
- 매입일 표시 — trading_performance 연동 필요

---

### M-2. 자산 스냅샷 저장 & 대시보드 차트 ✅ 구현 완료 (2026-05 완료)

**04-14 현황:** `res.json([])` — 빈 배열 반환  
**05-09 현황:** 스키마 → 저장 → API → 프론트 전부 연결

**구현된 내용:**
- 스키마: `asset_snapshots` 테이블 (schema.ts:926)
- 저장: BalanceRefreshService 잔고 갱신 시 자동 저장 (fire-and-forget, try/catch 격리)
- API: `GET /api/accounts/:accountId/asset-snapshots?days=30` (account.routes.ts:347-364)
- 프론트: dashboard.tsx에서 자산 추이 Recharts LineChart 연결 (dashboard.tsx:92, 264)

---

### M-3. 시장 이슈 종목 관리 ✅ API 구현 완료 (2026-05 완료)

**04-14 현황:** 테이블에 행 0개, 등록 UI 없음  
**05-09 현황:** CRUD API 4개 완전 구현

**구현된 내용 (formula.routes.ts:428-472):**
- `GET /api/market-issues?date=YYYYMMDD` — 날짜별 조회
- `GET /api/market-issues/stock/:code` — 종목별 조회
- `POST /api/market-issues` — 등록 (insertMarketIssueSchema 검증)
- `DELETE /api/market-issues/:id` — 삭제

**아직 미구현:**
- 전용 관리 UI 화면 (현재 API만 존재, 별도 화면에서 통합 호출 형태)
- 자동 등록 (backattack-scan 결과 연동, DART 공시 키워드 매칭)

---

### M-4. 시그널 자동 생성 ✅ 구현 완료 (2026-05 완료)

**04-14 현황:** `"시그널은 뒷차기 스캔 탭에서 실행하세요"` 안내 메시지만 반환  
**05-09 현황:** 실제 레인보우 분석 + upsert 완전 동작

**구현된 내용 (watchlist.routes.ts:296-380):**
- 관심종목 전체 순회
- 각 종목: 차트 데이터 조회 → `RainbowChartAnalyzer.analyze()` 실행
- 신호 판단: `currentPosition ≤ 30` → buy, `≥ 70` → sell, else → neutral
- 기존 시그널 있으면 update, 없으면 create
- 성공/실패 건수 응답 + 100ms 딜레이 (API 부하 방지)

---

### M-5. Pending 주문 자동 만료 ✅ 구현 완료

**04-14 현황:** 매매잡 의존 문제  
**05-09 현황:** expireOldPendingOrders() 정상 동작 확인 (tradingJob 내 호출)

---

### M-6. 거래 성과 추적 자동 저장 ✅ 정상 구현 확인 (변경 없음)

`executeBuy()` 내 `storage.createTradingPerformance()` 정상 호출.  
실거래 데이터 축적에 따라 정상 동작.

---

### M-7. GPT 프롬프트 사용자 편집 기능 ❌ 미구현

**현황:** 설계만 완료. `ai.service.ts`의 프롬프트는 코드에 하드코딩됨.

**구현 내용:**
```
1. DB: ai_models 또는 ai_model_specs에 analyzeStockPrompt, integratedAnalysisPrompt 컬럼 추가
2. ai.service.ts: 모델별 promptTemplate를 DB에서 읽어 사용
3. UI: 자동매매 설정 > AI 모델 탭에 "프롬프트 편집" 섹션 추가
```

**우선순위:** 낮음

---

### M-8. 3년 재무 추세 필터 ❌ 미구현

**현황:** 설계만 완료. `financial_snapshots` 테이블 존재, sync-financials API 구현됨.

**구현 내용:**
```
1. 스캔잡 실행 시 각 후보 종목의 재무제표 3년치 조회 + financial_snapshots 저장
2. 필터 조건: 매출 3년 우상향 + 영업이익 2년 개선 시 candidate 통과
3. AutoTradingSettings에 requireStrongFinancialTrend 토글 추가
```

**우선순위:** 낮음

---

### M-9. 거래량 스파이크 필터 ❌ 미구현

**현황:** 설계만 완료.

**구현 내용:**
```
1. 스캔잡 후보 종목에서 일봉 60일 조회
2. 최근 20일 내 거래량이 60일 평균 대비 N배 이상인 날이 있으면 통과
3. AutoTradingSettings에 volumeSpikeMultiplier 슬라이더 추가
```

**우선순위:** 낮음

---

## 4. 모니터링 뷰 — 04-14 → 05-09 구현 현황 업데이트

### 현재 화면 구조 (2026-05-09 사이드바 기준)

| 경로 | 페이지명 | 상태 |
|------|---------|------|
| `/` | 대시보드 | ✅ 계좌별 잔고·보유종목·자산추이차트 연동 완료 |
| `/trade-history` | 거래내역 | ✅ 5개 탭 (주문/로그/일별/월별/종목별) + 차트 |
| `/auto-trading` | 자동매매 | ✅ AI 모델 관리·설정·추천·학습 |
| `/portfolio` | 포트폴리오 | ✅ 전계좌 통합 보유종목 + 요약 카드 |
| `/monitoring` | 실시간 모니터 | ✅ 잡 상태·후보종목·매매결정피드·알림요약·학습요약 |
| `/admin-jobs` | 잡 관리 | ✅ 잡 ON/OFF·마지막 실행 시간 |

---

### MON-1. 실시간 자동매매 모니터링 화면 ✅ 구현 완료

**04-14 현황:** 콘솔 로그만 출력, 화면에서 볼 방법 없음  
**05-09 현황:** `monitoring.tsx` 493줄, 완전한 페이지

**구현된 내용:**
- 잡 상태 카드 3개: scan/trading/learning 상태, 마지막 실행, 오류 횟수
- 엔진 상태 배지: 가동중/정지 + 마지막 사이클 시각
- 알림 요약: 전체/미확인/긴급/경고 건수
- 학습 요약: 학습 플래그, 활성 모델, 승률, 평균수익, 자동반영 여부, 다음 실행
- 후보 종목 테이블: 종목명/코드, 레인보우 라인, 스캔시각, confidence/skipReason
- 매매 결정 피드: BUY/SELL/SKIP + confidence + 점수별 breakdown + 계산 근거
- 30초 자동갱신 (`refetchInterval: 30_000`)

---

### MON-2. 통합 보유종목 뷰 ✅ 구현 완료 (→ M-1과 동일)

포트폴리오 페이지에서 전계좌 통합 보유종목 완전 구현.  
상세 내용은 M-1 참조.

---

### MON-3. 거래 성과 일별/월별 조회 ✅ 구현 완료

**04-14 현황:** 날짜 필터 없음, 집계 없음  
**05-09 현황:** `trade-history.tsx` 632줄, 5개 탭

**구현된 내용:**
- 탭: [주문 내역] [거래 로그] [일별 성과] [월별 성과] [종목별 성과]
- 일별: 날짜 필터 + 바 차트 (손익 양/음 색상) + 테이블 (매수/매도/실현손익/승률)
- 월별: 누적 수익 라인 차트 + 테이블 (총거래/매수/매도/실현손익/승률)
- 종목별: 종목명, 거래 횟수, 평균 수익률, 총 손익, 승률
- 백엔드: `GET /api/trading-performance/summary?groupBy=day|month|stock` (SQL 집계 쿼리)
- 주문 API: startDate/endDate 파라미터 지원

---

### MON-4. 매수/매도 결정 근거 로그 ✅ 구현 완료

**04-14 현황:** engine_notifications 0행, console.log만  
**05-09 현황:** BUY/SELL/SKIP/ADDITIONAL_BUY/EXIT_SELL 이벤트 저장 + 모니터링 피드 연동

**구현된 내용:**
- engine_notifications에 매매 결정 이벤트 저장 (fire-and-forget)
- payload: confidence, themeScore, newsScore, financialsScore, liquidityScore, institutionalScore
- confidenceBreakdown: 가중합 계산 근거 (weights × scores / denominator)
- 모니터링 페이지에서 실시간 조회 + 렌더링

---

## 5. 구현 우선순위 및 일정 (2026-05-09 업데이트)

### ✅ 완료됨 — 04-14 기준 즉시/단기 항목 전부 구현 완료

| # | 항목 | 상태 |
|---|------|------|
| 1 | ~~M-5: pending 주문 자동 만료 타이머~~ | ✅ 완료 |
| 2 | ~~MON-4: trade-executor 매수/스킵/매도 notification 저장~~ | ✅ 완료 |
| 3 | ~~MON-1: 실시간 모니터링 페이지~~ | ✅ 완료 (493줄) |
| 4 | ~~MON-2: 포트폴리오 페이지~~ | ✅ 완료 (245줄) |
| 5 | ~~MON-3: 거래내역 일별/월별/종목별 탭~~ | ✅ 완료 (632줄) |
| 6 | ~~M-2: 자산 스냅샷 + 대시보드 차트~~ | ✅ 완료 |
| 7 | ~~M-3: 시장 이슈 API~~ | ✅ 완료 (CRUD 4개) |
| 8 | ~~M-4: 시그널 자동 생성~~ | ✅ 완료 |

### 🟡 아직 남은 부가 기능

| # | 항목 | 우선순위 | 예상 작업량 |
|---|------|---------|------------|
| 1 | M-3 부가: 시장 이슈 전용 관리 UI 화면 | 중간 | 소 (2~3시간) |
| 2 | M-1 부가: 포트폴리오 레인보우 라인 표시 | 낮음 | 중 (4시간) |
| 3 | M-1 부가: 섹터/업종별 분산 차트 | 낮음 | 중 (4시간) |

### 🟢 미구현 — 기능 확장 (변경 없음)

| # | 항목 | 우선순위 | 예상 작업량 |
|---|------|---------|------------|
| 4 | M-7: GPT 프롬프트 사용자 편집 | 낮음 | 대 (8시간) |
| 5 | M-8: 3년 재무 추세 필터 | 낮음 | 대 (8시간) |
| 6 | M-9: 거래량 스파이크 필터 | 낮음 | 중 (4시간) |

---

## 5-1. ⛔ 구현 시 안전 규칙 (기존 기능 파괴 방지)

아래 3가지를 반드시 지켜야 기존 매매 파이프라인·잔고 갱신이 깨지지 않음.

### 규칙 1: trade-executor 안의 notification / updateCandidateStock → fire-and-forget

```typescript
// ✅ 안전: fire-and-forget (notification 실패해도 매매 흐름 무영향)
if (marketAnalysis.confidence < minConf) {
  storage.createEngineNotification({...}).catch(e => console.error('[Notification]', e));
  return;
}

// ✅ 안전: 개별 try/catch (notification 실패 격리)
if (marketAnalysis.confidence < minConf) {
  try { await storage.createEngineNotification({...}); } catch(e) { console.error(e); }
  return;
}
```

### 규칙 2: BalanceRefreshService 스냅샷 저장 → 별도 try/catch 격리

✅ 현재 코드에 이미 적용됨 (balance-refresh.service.ts:196-205):
```typescript
try {
  await storage.createAssetSnapshot({...});
} catch (e) {
  console.error('[BalanceRefresh] failed to create asset snapshot (non-fatal):', e);
}
```

### 규칙 3: candidate_stocks 신규 컬럼 → 반드시 nullable

✅ 현재 코드에 이미 적용됨 (evaluationResult, skipReason, evaluatedAt 모두 nullable)

---

## 6. 변경 이력

| 날짜 | 내용 |
|------|------|
| 2026-04-14 | 최초 작성 — 소스코드 + DB 직접 감사 기반 11개 미구현 항목 식별 |
| 2026-05-09 | **전면 업데이트** — 코드 실측 결과 8개 항목 구현 완료 확인. M-7/M-8/M-9만 잔여. Replit→로컬 전환 반영 |

---

## 7. 건드리지 않는 것 (이미 정상)

| 파일/기능 | 이유 |
|-----------|------|
| `server/formula/rainbow-chart.ts` | 알고리즘 정확, 변경 없음 |
| `server/services/ai.service.ts` | GPT 호출 정상 |
| `server/services/trade-executor.service.ts` | evaluateCandidateStock 구현 정상 |
| `server/auto-trading-worker.ts` | 잡 스케줄러 정상 |
| `server/services/learning.service.ts` | optimizeModel() 정상 |
| `server/services/order-sync.service.ts` | 동기화 로직 정상 |
| `shared/schema.ts` | DB 스키마 완성 |
