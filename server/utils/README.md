# server/utils/

## 역할
서버 전역에서 재사용되는 유틸리티 함수 모음.

## 주요 파일
| 파일 | 역할 |
|------|------|
| `crypto.ts` | AES-256-GCM 기반 API 키 암호화/복호화 (키움 앱키 저장용) |

## 규칙
- 특정 도메인에 종속되지 않는 순수 유틸리티 함수만 배치
- 외부 API 호출 금지 (순수 함수 원칙)
