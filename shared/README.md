# shared/

## 역할
클라이언트와 서버가 공유하는 타입, 스키마 정의.

## 주요 파일
| 파일 | 역할 |
|------|------|
| `schema.ts` | Drizzle ORM 테이블 정의 + Zod 검증 스키마 + TypeScript 타입 |

## 규칙
- 순수 타입/스키마만 정의 (로직 금지)
- 서버/클라이언트 양쪽에서 import 가능해야 함
- Node.js 전용 모듈(fs, path 등) 사용 금지
